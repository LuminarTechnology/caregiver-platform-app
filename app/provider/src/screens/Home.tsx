import { View, Text } from 'react-native'

const HomeScreen = () => {
  return (
    <View className="bg-primary flex h-screen items-center justify-center">
      <Text className="text-foreground text-center text-3xl font-semibold">
        Caregiver Provider
      </Text>
    </View>
  )
}

export default HomeScreen
